# SMTP for Cloud Functions (production email). Copy from repo root .env or set in Google Cloud.
# These are SERVER secrets — never put SMTP_PASS in VITE_* vars.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
HOTEL_NAME=Tabarak Hotel & Restaurant
